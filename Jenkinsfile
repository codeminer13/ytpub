@Library('pipleline-library-demo')_


pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                echo 'Hello, world'
                sayHello 'Dave'

            }
        }
    }
}
